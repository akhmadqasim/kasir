---
name: dev
description: Start the Tauri development server
user_invocable: true
---

Start the development environment:

1. Run `bun run tauri dev` to start both frontend and Tauri backend
2. If it fails, check and report the error
3. Do NOT run in background — keep the dev server in foreground
