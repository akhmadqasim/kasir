{
  "name": "@kasir/whatsapp-sidecar",
  "version": "0.1.0",
  "private": true,
  "description": "Child process bridging the kasir Rust backend to WhatsApp Web via whatsapp-web.js, over stdin/stdout JSON lines. Run by Rust with `node`, never by the frontend.",
  "main": "dist/index.js",
  "scripts": {
    "build": "node build.mjs",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "whatsapp-web.js": "^1.34.7"
  },
  "devDependencies": {
    "@types/node": "^24.12.0",
    "esbuild": "^0.27.0",
    "typescript": "~5.9.3",
    "vitest": "^4.1.0"
  }
}
